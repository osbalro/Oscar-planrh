# Oscar-planrh
# En tu máquina
git init
git add .
git commit -m "Initial backend"

# Crear repo en GitHub (privado está bien)
gh repo create conversor-freebets-backend --private --push --source .
# o manualmente desde github.com
